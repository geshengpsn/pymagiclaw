[package]
name = "pymagiclaw"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "pymagiclaw"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
numpy = "0.21"
libfranka-rs = "0.9"
nalgebra = "0.33.0"
# magiclaw-rs = { path = "../magiclaw-rs" }